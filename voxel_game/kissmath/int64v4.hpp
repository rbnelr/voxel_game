// file was generated by kissmath.py at <TODO: add github link>
#pragma once

#include "int64.hpp"

namespace kissmath {
	//// forward declarations
	
	struct bool4;
	struct int64v2;
	struct uint8v4;
	struct float4;
	struct int64v3;
	struct int4;
	
	struct int64v4 {
		union { // Union with named members and array members to allow vector[] operator, not 100% sure that this is not undefined behavoir, but I think all compilers definitely don't screw up this use case
			struct {
				int64	x, y, z, w;
			};
			int64		arr[4];
		};
		
		// Component indexing operator
		int64& operator[] (int i);
		
		// Component indexing operator
		int64 const& operator[] (int i) const;
		
		
		// uninitialized constructor
		int64v4 ();
		
		// sets all components to one value
		// implicit constructor -> float3(x,y,z) * 5 will be turned into float3(x,y,z) * float3(5) by to compiler to be able to execute operator*(float3, float3), which is desirable
		// and short initialization like float3 a = 0; works
		int64v4 (int64 all);
		
		// supply all components
		int64v4 (int64 x, int64 y, int64 z, int64 w);
		
		// extend vector
		int64v4 (int64v2 xy, int64 z, int64 w);
		
		// extend vector
		int64v4 (int64v3 xyz, int64 w);
		
		
		//// Truncating cast operators
		
		// truncating cast operator
		explicit operator int64v2 () const;
		
		// truncating cast operator
		explicit operator int64v3 () const;
		
		
		//// Type cast operators
		
		// type cast operator
		explicit operator bool4 () const;
		
		// type cast operator
		explicit operator uint8v4 () const;
		
		// type cast operator
		explicit operator float4 () const;
		
		// type cast operator
		explicit operator int4 () const;
		
		
		// componentwise arithmetic operator
		int64v4 operator+= (int64v4 r);
		
		// componentwise arithmetic operator
		int64v4 operator-= (int64v4 r);
		
		// componentwise arithmetic operator
		int64v4 operator*= (int64v4 r);
		
		// componentwise arithmetic operator
		int64v4 operator/= (int64v4 r);
		
	};
	
	//// arthmethic ops
	
	int64v4 operator+ (int64v4 v);
	
	int64v4 operator- (int64v4 v);
	
	int64v4 operator+ (int64v4 l, int64v4 r);
	
	int64v4 operator- (int64v4 l, int64v4 r);
	
	int64v4 operator* (int64v4 l, int64v4 r);
	
	int64v4 operator/ (int64v4 l, int64v4 r);
	
	
	//// bitwise ops
	
	int64v4 operator~ (int64v4 v);
	
	int64v4 operator& (int64v4 l, int64v4 r);
	
	int64v4 operator| (int64v4 l, int64v4 r);
	
	int64v4 operator^ (int64v4 l, int64v4 r);
	
	
	//// comparison ops
	
	// componentwise comparison returns a bool vector
	bool4 operator< (int64v4 l, int64v4 r);
	
	// componentwise comparison returns a bool vector
	bool4 operator<= (int64v4 l, int64v4 r);
	
	// componentwise comparison returns a bool vector
	bool4 operator> (int64v4 l, int64v4 r);
	
	// componentwise comparison returns a bool vector
	bool4 operator>= (int64v4 l, int64v4 r);
	
	// componentwise comparison returns a bool vector
	bool4 operator== (int64v4 l, int64v4 r);
	
	// componentwise comparison returns a bool vector
	bool4 operator!= (int64v4 l, int64v4 r);
	
	// vectors are equal, equivalent to all(l == r)
	bool equal (int64v4 l, int64v4 r);
	
	// componentwise ternary (c ? l : r)
	int64v4 select (bool4 c, int64v4 l, int64v4 r);
	
	
	//// misc ops
	// componentwise absolute
	int64v4 abs (int64v4 v);
	
	// componentwise minimum
	int64v4 min (int64v4 l, int64v4 r);
	
	// componentwise maximum
	int64v4 max (int64v4 l, int64v4 r);
	
	// componentwise clamp into range [a,b]
	int64v4 clamp (int64v4 x, int64v4 a, int64v4 b);
	
	// componentwise clamp into range [0,1] also known as saturate in hlsl
	int64v4 clamp (int64v4 x);
	
	// get minimum component of vector, optionally get component index via min_index
	int64 min_component (int64v4 v, int* min_index=nullptr);
	
	// get maximum component of vector, optionally get component index via max_index
	int64 max_component (int64v4 v, int* max_index=nullptr);
	
	
	// componentwise wrap
	int64v4 wrap (int64v4 v, int64v4 range);
	
	// componentwise wrap
	int64v4 wrap (int64v4 v, int64v4 a, int64v4 b);
	
	
	
	//// Vector math
	
	// squared magnitude of vector, cheaper than length() because it avoids the sqrt(), some algorithms only need the squared magnitude
	int64 length_sqr (int64v4 v);
	
	// dot product
	int64 dot (int64v4 l, int64v4 r);
	
}

